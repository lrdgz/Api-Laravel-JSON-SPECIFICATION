models:
  Article:
    title: string
    slug: string unique
    content: longtext
    category_id: id
    user_id: id
  Category:
    title: string
    slug: string unique
    relationships:
      hasMany: Article
